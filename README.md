### First step after using the template

1. Change the project name in `package.json` from `app` to your desired project name.
2. Change the compose project name, network name, database name (optional) and service name from `app` and `app_db_name` to your 
   desired project name in `docker-compose.yml`.
